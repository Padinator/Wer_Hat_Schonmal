<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ichhabschonmal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IchHabSchonMal">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewGame"/>
        <activity android:name=".CreatePlayers"/>
        <activity android:name=".PlayGame"/>
        <activity android:name=".LoadGame"/>
        <activity android:name=".Rules"/>
        <activity android:name=".Score"/>
        <activity android:name=".EndScore"/>
        <activity android:name=".EndScoreViewAllStories"/>


    </application>

</manifest>